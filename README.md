# Simple Calculator

A minimal command-line calculator with add, subtract, multiply, and divide.

## Usage

```bash
python3 calculator.py
```

Enter expressions in the form `<number> <op> <number>`, e.g. `3 + 4`. Ctrl+C to quit.

## Tests

```bash
python3 -m unittest test_calculator.py -v
```
